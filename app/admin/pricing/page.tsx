import { AdminPricingSettings } from "@/components/AdminPricingSettings";

export const metadata = {
  title: "Pricing & Shipping Rules | Admin Panel",
};

export default function AdminPricingPage() {
  return <AdminPricingSettings />;
}
